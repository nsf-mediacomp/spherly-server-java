# -*- mode: python -*-
a = Analysis(['SpherlyServer.py'],
             pathex=['D:\\cake\\UA\\NSF-UA-Gray\\spherly\\server\\__Linux'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='SpherlyServer.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='SpherlyServer')
